//Iterating

// forEach() method that executes a fuctionjonce for rach array element

//forEach has 3 values
forEach('currentValue', 'the index', 'the entire array');

let food = ['Pecan pie', 'Shrimp', 'Quesadilla', 'Cheese cake', 'Hotdog'];
//food.forEach(f => {console.log(ff)}) //consise body
food.forEach((f, mumber) => {
    console.log(number, f)
}) //fat arrow
food.forEach({
    f,
    number,
    array) => {
    console.log(f, number.arry)
}) //come back and wite out outcome


let foodFunction = () => {
    for (f in food) {
        console.log(f),
            console.log(food[f])

    }
}




/**
 Challenge:
 Create a list (with an array) of movies
 Use .forEach() to list your movies
 Add another movie at the end
 And repalce one of your existing movie with another one
 */


let movies = ['Matrix', 'Rambo', 'Donnie Darko'];

movies.forEach(movies => {
    console.log(movie)])
movies.push('Rambo');
movies.splice(0, 1, 'Matrix 2');
movies.push(movie + {})
}

})